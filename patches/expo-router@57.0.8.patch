diff --git a/build/react-navigation/stack/views/Stack/Card.js b/build/react-navigation/stack/views/Stack/Card.js
index 4846a1e833e2e92669f1d67e08f5673afec8d63c..78522486b642f52c1ca08d58e9d8912bce15080c 100644
--- a/build/react-navigation/stack/views/Stack/Card.js
+++ b/build/react-navigation/stack/views/Stack/Card.js
@@ -53,6 +53,12 @@ const color_1 = require("../../../../utils/color");
 const GESTURE_VELOCITY_IMPACT = 0.3;
 const TRUE = 1;
 const FALSE = 0;
+/** Local replacement for deprecated `InteractionManager` create/clear handles. */
+let nextInteractionHandle = 0;
+function createInteractionHandle() {
+    return ++nextInteractionHandle;
+}
+function clearInteractionHandle(_handle) { }
 const useNativeDriver = react_native_1.Platform.OS !== 'web';
 const hasOpacityStyle = (style) => {
     if (style) {
@@ -84,12 +90,12 @@ function Card({ shadowEnabled = false, gestureEnabled = true, gestureVelocityImp
     const [isSwiping] = React.useState(() => new react_native_1.Animated.Value(FALSE));
     const onStartInteraction = (0, useLatestCallback_1.default)(() => {
         if (interactionHandleRef.current === undefined) {
-            interactionHandleRef.current = react_native_1.InteractionManager.createInteractionHandle();
+            interactionHandleRef.current = createInteractionHandle();
         }
     });
     const onEndInteraction = (0, useLatestCallback_1.default)(() => {
         if (interactionHandleRef.current !== undefined) {
-            react_native_1.InteractionManager.clearInteractionHandle(interactionHandleRef.current);
+            clearInteractionHandle(interactionHandleRef.current);
             interactionHandleRef.current = undefined;
         }
     });

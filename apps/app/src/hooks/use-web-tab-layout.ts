/**
 * @deprecated Prefer {@link useLargeScreenLayout} — kept as a stable alias for
 * existing web chrome callers (mini-player, content insets).
 */
export {
  getWebBottomTabBarHeight,
  LIST_DETAIL_BREAKPOINT,
  SIDE_RAIL_BREAKPOINT,
  SIDE_RAIL_WIDTH,
  useLargeScreenLayout as useWebTabLayout,
} from "@/hooks/use-large-screen-layout";

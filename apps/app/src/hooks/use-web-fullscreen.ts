/**
 * @deprecated Import from `@/hooks/use-reading-fullscreen` instead.
 * Re-export kept so existing imports keep working during the native immersive rollout.
 */
export { useReadingFullscreen, useWebFullscreen } from "@/hooks/use-reading-fullscreen";

/**
 * Native no-op stub. The web variant (`focus-visible.web.ts`) injects a
 * global focus-visible ring stylesheet; there is nothing to do on native,
 * where focus rings are handled by the platform.
 */
export {};
